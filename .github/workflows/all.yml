name: CI

on:
  # Enable manually triggering this workflow via the API or web UI
  workflow_dispatch:
  push:
    branches:
      - main
    tags:
      - v*
  pull_request:

permissions: {}

jobs:
  k6-ci:
    permissions:
      contents: read
    uses: grafana/k6-ci/.github/workflows/all.yml@a7138bd426a25e8472abf4e8b37f2ab9f09c866a # v0.2.0
    with:
      k6-ci-ref: a7138bd426a25e8472abf4e8b37f2ab9f09c866a # v0.2.0
